# Source Cards

Source cards summarize benchmark sources, model-family sources, or other
external references in a stable format.

## Folders

- `openml/`: OpenML dataset cards used by the local benchmark suite.

## Rule

Do not compare reported numbers across cards unless dataset, target, split,
feature exclusions, preprocessing, model parameters, metric, and runtime
environment match. Otherwise cite them only as dataset or related-work context.
