# Model Experiment Notes

Use this folder for model-family or wrapper behavior notes that are not tied to
one dataset.

Examples:

- wrapper capability checks
- external package behavior differences
- model-specific runtime failure modes
- model-family selection decisions

Do not place dataset leaderboard claims here. Dataset-specific numeric claims
belong under `experiment_notes/datasets/`.

