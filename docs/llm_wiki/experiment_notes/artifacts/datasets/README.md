# Dataset Artifacts

Store sanitized dataset-scoped result snapshots here.

Create one subfolder per dataset slug before adding artifacts. Keep raw ignored
result files outside the wiki, and copy only the rows needed to support a wiki
note.

